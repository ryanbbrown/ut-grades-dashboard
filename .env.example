# S3-compatible storage credentials (works with AWS S3, Cloudflare R2, MinIO, etc.)

# Access credentials
S3_ACCESS_KEY_ID=your_access_key_id_here
S3_SECRET_ACCESS_KEY=your_secret_access_key_here

# Bucket configuration
S3_BUCKET_NAME=bucket-name

# S3 Endpoint URL (optional - only needed for non-AWS S3-compatible services)
# For Cloudflare R2: https://<account_id>.r2.cloudflarestorage.com
# For MinIO: http://localhost:9000
# Leave blank or omit for AWS S3
S3_ENDPOINT_URL=

# Region (optional - defaults to us-east-1 if not specified)
S3_REGION=auto
